// ══════════════════════════════════════════════════════════════
//  truss_config.js — הגדרת הבעיה המשותפת ל-truss_solver.js וב-server.js
//  שנה כאן בלבד — השרת והסולבר יתעדכנו אוטומטית
// ══════════════════════════════════════════════════════════════

// ── 1. צמתים  [X, Y]  (מטרים) ────────────────────────────────
const NODES = [
  //    x     y
  [   0,    0  ],  // צומת 0
  [ 100,    100  ],  // צומת 1
  [  100,  0  ],  // צומת 2  ← עומס
  [  0,  100  ],
];

// ── 2. קורות  [צומת_א, צומת_ב] ───────────────────────────────
const MEMBERS = [
  [ 0, 2 ],   // קורה 0
  [ 0, 1 ],   // קורה 1
  [ 1, 2 ],
  [ 1, 3 ],
  [ 2, 3 ],
];

// ── 3. ריתומים — כל צומת שנוגעת בקו תחשב לרתומה (pin) ────────
//    הגדר null לכל קו שאינו פעיל
const BOUNDARY = {
  x_min:    0,    // קו אנכי שמאלי    (כל x ≤ x_min  רתום)
  x_max:  null,    // קו אנכי ימני     (כל x ≥ x_max  רתום)
  y_min:  null,   // קו אופקי תחתון   (כל y ≤ y_min  רתום)
  y_max:  null,   // קו אופקי עליון   (כל y ≥ y_max  רתום)
};

// ── 4. עומסים  [ מספר_צומת,  Fx,   Fy ] ─────────────────────
const LOADS = [
  [ 2,  0,  -1000 ],   // 1000 N כלפי מטה בצומת 2
];

// ── 5. תכונות חומר וחתך (לsolver בלבד) ──────────────────────
const ROD_SIDE  = 0.002;   // צלע חתך מרובע [מ']
const YIELD_MPa = 276;     // גבול כניעה  [MPa]  (פלדה A36)
const E_PA      = 200e9;   // מודול אלסטיציות [Pa]

// ─────────────────────────────────────────────────────────────
module.exports = { NODES, MEMBERS, BOUNDARY, LOADS, ROD_SIDE, YIELD_MPa, E_PA };
